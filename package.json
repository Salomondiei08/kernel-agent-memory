{
  "name": "kernel",
  "version": "0.0.1",
  "description": "Unified multi-agent memory & token tracking tool",
  "main": "dist/index.js",
  "bin": {
    "kernel": "dist/cli.js"
  },
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "js-tiktoken": "^1.0.10"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
